AWSTemplateFormatVersion: 2010-09-09
Description: >
  ...

Parameters:

  SecretName:
    Type: String
    Description: Secret Name 
    Default: /secret/liriotech_dev 
    # Default: /secret/av2_dev
    # Default: /secret/av2_hom
    # Default: /secret/av2_prod

  SecretString:
    Type: String
    Description: Secret String
    Default: '{"redis.auth":"123456"}'

Resources:
  LirioTechSecretsManagerCredentials:
    Type: AWS::SecretsManager::Secret
    Properties: 
      Description: Lirio Credentials 
      #GenerateSecretString: 
      #  GenerateSecretString
      #KmsKeyId: String
      Name: !Ref SecretName
      #SecretString: 
      #  String
      SecretString: !Ref SecretString
      Tags: 
        - Key: "Name"
          Value: "LirioTech"
        - Key: "ENV"
          Value: "Dev"

  ClienteSecretsManagerCredentials:
    Type: AWS::SecretsManager::Secret
    Properties: 
      Description: Cliente Credentials
      #GenerateSecretString: 
      #  GenerateSecretString
      #KmsKeyId: String
      Name: !Ref SecretName
      #SecretString: 
      #  String
      SecretString: '{"token.client":"5w614g3rteh","token.secret":"123654987"}'
      Tags: 
        - Key: "Name"
          Value: "LirioTech"
        - Key: "ENV"
          Value: "Dev"      
        - Key: "Service"
          Value: "Cliente"              

# Ref.: https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-secretsmanager-secret.html          